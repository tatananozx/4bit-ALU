 
# 4 bit ALU

# 4bit-ALU
A 4-bit ALU in Verilog supporting 8 operations (ADD, SUB, AND, OR, XOR, NOT, left shift, right shift), with a shared adder/subtractor module using two's complement control logic.

